1) Recursion. Fun and weird thing, may be helpful sometimes.
2) Why in recursion function having 2 returns, but it returns not "0" but result of recursion.
	int add(int n)
	{
        	if(n > 0) return n + add(n-1);
        	return 0;
	}
3)

